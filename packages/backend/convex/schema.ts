import { defineSchema } from "convex/server";

// Rien pour l'instant : un site vitrine n'a pas de données tant qu'il n'a pas
// de formulaire ni de contenu éditable.
export default defineSchema({});
